# 发布人工检查单

覆盖 `RELEASE_GATES.md` 的 **W-01、W-02、W-03、W-06**。这四项**必须在真实目标环境
实测**，不能用开发机结果推断，也不能用合成测试替代。

> **红线**
> - 在本检查单全部通过、且研究门禁 R-01 ~ R-07 完成后，才能把 NSIS 资产标记为
>   正式稳定发行版。当前候选安装包只是**工程测试版本**。
> - 任何一项未通过都要如实记录，不得勾选。
> - 每项记录执行人、日期、机器标识与原始日志路径。

## 0. 被测构建

| 字段 | 值 |
| --- | --- |
| 安装包 | `<...>` |
| SHA-256 | `<...>` |
| 文件大小 | `<...>` |
| 构建提交号 | `<...>` |
| 构建日期 | `<...>` |

**注意**：重新构建会改变 SHA-256。每次实测都必须重新记录，不得沿用旧哈希。
历史候选：`医疗影像智能识别平台 Setup 0.1.0.exe`，
SHA-256 `276E8578DFFA79711CBB6046EAE1F00543DA6247E3AF512C3302640C866E7972`（2026-09-19）。

测试用 `.medmodel` 必须是**许可明确**的真实模型；不得用合成测试模型冒充，
也不得在报告中陈述任何准确率结论。若此时还没有真实权重，W-01/W-02 的
"推理"步骤无法完成，只能如实标注为未完成。

---

## W-01 干净 Windows 10 x64

**前置条件**：全新虚拟机或全新安装的 Windows 10 x64，**未安装 Python、Node.js、
NVIDIA 驱动**。建议先打快照，便于重复验证与卸载测试。

| 步骤 | 操作 | 通过标准 | 结果 |
| --- | --- | --- | --- |
| 1 | 记录系统版本与是否装有 Python / Node / CUDA | 三者均无 | `<...>` |
| 2 | 双击安装包，选择自定义安装目录 | 安装向导正常，目录可选择 | `<...>` |
| 3 | 安装完成，从开始菜单启动 | 能启动，无缺失 DLL 报错 | `<...>` |
| 4 | 首次启动创建管理员 | 成功创建，未出现明文密码 | `<...>` |
| 5 | 管理员创建独立医生账号 | 成功，权限符合角色矩阵 | `<...>` |
| 6 | 无模型时进入阅片 | 显示"尚未安装模型"并**拒绝预测** | `<...>` |
| 7 | 导入许可明确的 `.medmodel` | 校验通过，激活成功 | `<...>` |
| 8 | 导入一张合成或授权 DICOM，完成去标识化入库 | 成功，无原始路径残留 | `<...>` |
| 9 | 执行 CPU 推理 | 成功产出 14 项概率与 CAM | `<...>` |
| 10 | 医生复核并确认报告 | 成功，版本锁定 | `<...>` |
| 11 | 导出中文 PDF | 成功，含报告正文与 AI 附录 | `<...>` |
| 12 | 卸载 | 卸载完成，无残留进程 | `<...>` |
| 13 | 卸载后检查数据保留策略 | 与第 3 节声明的策略一致 | `<...>` |
| 14 | 检查 SmartScreen / 签名提示 | 与第 3 节签名状态一致 | `<...>` |

执行人 / 日期：`<...>`　系统版本：`<...>`　快照名：`<...>`

---

## W-02 干净 Windows 11 x64

与 W-01 步骤完全相同，单独记录（**不得复制 W-01 的结果**）。

| 步骤 | 通过标准 | 结果 |
| --- | --- | --- |
| 1–14 | 同 W-01 | `<...>` |

执行人 / 日期：`<...>`　系统版本：`<...>`　快照名：`<...>`

**Win10 与 Win11 的差异点需特别留意**：安装目录权限、SmartScreen 行为、
杀软拦截提示、开始菜单与卸载入口位置。

---

## W-03 Ryzen 5 5600 单张 CPU 推理性能

目标：**单张推理不超过 15 秒**。

**测量条件**（必须记录，否则数字不可比）：

| 项目 | 值 |
| --- | --- |
| CPU | AMD Ryzen 5 5600 |
| 内存 | `<...>` |
| 操作系统 | `<...>` |
| 模型 | `<...>`（许可明确的最终模型） |
| ONNX Runtime 版本 | `<...>` |
| ONNX Runtime provider | `CPUExecutionProvider` |
| 图像尺寸 | 320×320 |
| 线程数设置 | `<...>` |

| 指标 | 要求 | 实测 |
| --- | --- | --- |
| 冷启动（首张，含会话初始化） | 记录即可，不设上限 | `<...>` 秒 |
| 单张推理中位数（≥30 张） | — | `<...>` 秒 |
| 单张推理 P95（≥30 张） | ≤ 15 秒 | `<...>` 秒 |
| 峰值内存占用 | 记录即可 | `<...>` |

记录原始日志路径：`<...>`
执行人 / 日期：`<...>`

**注意**：测量前关闭其它占用 CPU 的程序；同一张图重复测不得命中缓存，
需使用不同影像；若超过 15 秒，如实记录并说明，不得四舍五入到"通过"。

---

## W-06 签名、卸载数据策略与第三方许可

### W-06.1 Authenticode 代码签名

| 检查 | 通过标准 | 结果 |
| --- | --- | --- |
| 安装包已签名 | `Get-AuthenticodeSignature` 显示 `Valid` | `<...>` |
| 签名主体与证书一致 | 与预期发布者一致 | `<...>` |
| 时间戳存在 | 证书有效期外仍可验证 | `<...>` |
| 内嵌服务可执行文件已签名 | 同样 `Valid` | `<...>` |
| 目标机 SmartScreen 行为 | 已记录实际提示 | `<...>` |

签名后安装包 SHA-256 会改变，**必须重新记录**并重新执行 W-01 / W-02 的安装步骤。

### W-06.2 卸载数据保留策略

必须**分别实测**"保留本地数据"与"清理"两种行为，不得只测一种。

| 检查 | 通过标准 | 结果 |
| --- | --- | --- |
| 卸载前记录数据目录路径 | `<...>` | `<...>` |
| 卸载后数据库是否存在 | 与声明的策略一致 | `<...>` |
| 卸载后去标识化影像是否存在 | 与声明的策略一致 | `<...>` |
| 卸载后模型文件是否存在 | 与声明的策略一致 | `<...>` |
| 卸载后密钥 / `safeStorage` 数据是否清除 | 与声明的策略一致 | `<...>` |
| 文档声明与实测行为一致 | 完全一致 | `<...>` |

**声明的策略**（须与 `docs/SECURITY.md` 一致）：`<...>`

### W-06.3 第三方许可人工复核

| 检查 | 通过标准 | 结果 |
| --- | --- | --- |
| `third_party/NOTICE.md` 覆盖全部打包组件 | 逐项可对应 | `<...>` |
| 每个组件许可证类型已记录 | 无"未知" | `<...>` |
| Copyleft 组件（GPL/AGPL 等）是否混入 | 若存在必须评估再分发后果 | `<...>` |
| Electron / Cornerstone3D / ONNX Runtime / PyInstaller 等主要依赖 | 许可证与声明一致 | `<...>` |
| 安装包内 `resources/legal` 内容完整 | 含 `LICENSE.txt` 与 `third_party` | `<...>` |
| 参考项目（TorchXRayVision、MixStyle 等）只借鉴设计 | **未复制许可不明代码** | `<...>` |
| CheXpert / MIMIC / ImageNet 条款已核对 | 各自原始条款已记录 | `<...>` |
| 最终模型权重的再分发许可 | 明确允许或明确不发布权重 | `<...>` |

复核人 / 日期：`<...>`

---

## 汇总

| 门禁 | 结论 | 证据路径 | 执行人 | 日期 |
| --- | --- | --- | --- | --- |
| W-01 | `<...>` | `<...>` | `<...>` | `<...>` |
| W-02 | `<...>` | `<...>` | `<...>` | `<...>` |
| W-03 | `<...>` | `<...>` | `<...>` | `<...>` |
| W-06 | `<...>` | `<...>` | `<...>` | `<...>` |

四项全部通过后，把结论回填到 `docs/RELEASE_GATES.md`，再考虑创建
`v0.1.0` Release。
